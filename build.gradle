apply plugin: 'idea'
apply plugin: 'java'

version = '0.6'

repositories {
	mavenCentral()
	mavenLocal()
	maven { url "https://hub.spigotmc.org/nexus/content/repositories/snapshots/" }
	maven { url "https://oss.sonatype.org/content/repositories/snapshots/" }
	maven { url "http://repo.dmulloy2.net/nexus/repository/public/" }
	maven { url "http://repo.md-5.net/content/groups/public/" }
	maven { url "http://maven.sk89q.com/repo/" }
	flatDir {
		dirs 'libs'
		dirs 'atlacore/atlacore-bukkit/bin/libs'
		dirs 'atlacore/atlacore-bukkit/libs'
	}
}

sourceSets {
	main {
		java {
			srcDirs = ['src/main/java']
		}
		resources {
			srcDirs = ['src/main/resources']
		}
	}
}

processResources {
	filter {
		line ->
		line
		.replace('${name}', project.name)
		.replace('${version}', project.version)
	}
}

jar {
	manifest {
		attributes 'Implementation-Title': project.name,
		           'Implementation-Version': project.version
	}
}

dependencies {
	compile name: 'atlacore-bukkit-0.7.2'
	compile name: 'MobArena'
	compile name: 'Towny-0.90'
	compile 'com.sk89q.worldedit:worldedit-core:7.2.8'
	compile 'com.sk89q.worldedit:worldedit-bukkit:7.2.8'
	compile 'com.sk89q.worldguard:worldguard-core:7.0.2'
	compile 'LibsDisguises:LibsDisguises:9.+'
	compile 'com.comphenix.protocol:ProtocolLib:4.+'
	compile 'com.projectkorra:projectkorra:1.8.4.44'
	compile 'org.spigotmc:spigot-api:1.18.1-R0.1-SNAPSHOT'
}

buildDir = 'bin'
